version: 2.1

jobs:
  build:
    docker:
      - image: 'circleci/openjdk:11-jdk'
    steps:
      - checkout
      - run:
          name: Analyze with SonarCloud
          command: |
            ./sonar-scanner-4.5.0.2216-linux/bin/sonar-scanner \
              -Dsonar.projectKey=Saksdotcom \
              -Dsonar.organization=Saksdotcom \
              -Dsonar.sources=. \
              -Dsonar.host.url=https://sonarcloud.io \
              -Dsonar.login=1db1b549abda6130a920bf0f301594d025b9927e \

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud 